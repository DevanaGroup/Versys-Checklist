rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras para usuários
    match /users/{userId} {
      allow read, write: if request.auth != null && (
        request.auth.uid == userId ||
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin'
      );
    }
    
    // Regras para clientes
    match /clientes/{clienteId} {
      allow read, write: if request.auth != null && (
        request.auth.uid == clienteId || 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin'
      );
    }
    
    // Regras para colaboradores
    match /colaboradores/{colaboradorId} {
      allow read, write: if request.auth != null && (
        request.auth.uid == colaboradorId || 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin'
      );
    }
    
    // Regras para projetos - PERMITIR ACESSO TOTAL PARA USUÁRIOS AUTENTICADOS
    match /projetos/{projectId} {
      allow read, write: if request.auth != null;
    }
    
    // Regras para presets
    match /presets/{presetId} {
      allow read, write: if request.auth != null && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin';
    }
    
    // Regras para itens
    match /itens/{itemId} {
      allow read, write: if request.auth != null;
    }
    
    // Regras para relatórios
    match /relatorios/{relatorioId} {
      // Permitir leitura se for o cliente dono do relatório ou admin
      allow read: if request.auth != null && (
        resource.data.clientId == request.auth.uid ||
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin'
      );
      
      // Permitir criação se o clientId no documento for o mesmo do usuário autenticado ou for admin
      allow create: if request.auth != null && (
        request.resource.data.clientId == request.auth.uid ||
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin'
      );
      
      // Permitir atualização se for o cliente dono do relatório ou admin
      allow update: if request.auth != null && (
        resource.data.clientId == request.auth.uid ||
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin'
      );
      
      // Permitir deleção se for o cliente dono do relatório ou admin
      allow delete: if request.auth != null && (
        resource.data.clientId == request.auth.uid ||
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin'
      );
    }
    
    // Regras para resumos de relatórios
    match /relatorio_summaries/{summaryId} {
      allow read, write: if request.auth != null && (
        resource.data.clientId == request.auth.uid ||
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.type == 'admin'
      );
    }
  }
} 